import React, { useContext, useEffect } from "react";
import { View, Text } from "react-native";

function PaymentScreen({navigation}) {
    return (
            <Text>"You are on Payment screen"</Text>
    );
}

export default PaymentScreen;